node_modules/
.next/
out/
# data/ is no longer used — storage is Postgres + Vercel Blob
.env
.env*.local
next-env.d.ts
tsconfig.tsbuildinfo
*.log

# Scratch build for tests/review-live.mjs
.trc-live-*/
.trc-golden-*/
.trc-corpus-*/

# Test harnesses compile the real modules into a scratch dir inside the
# project, because npm packages resolve by walking up from the importing file.
# Removed on exit; ignored in case a run is interrupted.
.tmp-*/
